export enum ErrorMessage {
    InvalidModel = 'Model invalid.',
    RecordNotFound = 'Unable to find record.'
}

export enum HttpStatus {
    BadRequest = 400,
    Success = 200,
    ServerError = 500,
    NotFound = 400
}
